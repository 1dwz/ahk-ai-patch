# Build outputs
upstream/bin/
upstream/bin_*/
upstream/temp/
upstream/obj/
upstream/*.vcxproj.user
upstream/AutoHotkeyx.sdf
upstream/*.opensdf
upstream/*.suo
upstream/ipch/

# Local staging
dist/
out/
*.zip

# Release assembly (tools/pack-release.ps1 output) and its per-release notes
release/
release-notes.md

# Pristine control tree/binaries for tools/test-parity.ps1 (built from the same
# pin; regenerated on demand, never committed)
pristine-test/
pristine-dist/

# Editor / OS
.vs/
.vscode/
*.swp
Thumbs.db
desktop.ini

# Downloaded CI artifacts
download/

# Agent private workspace
.pi/memory/

# Stock binaries kept for revert (large, machine-specific)
backup-stock/


# scratch scripts used while testing
/*.ahk


# commit message scratch
commitmsg.txt
